[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise-adk-workflow"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "google-adk==2.6.1",
]

[tool.setuptools.packages.find]
where = ["src"]
