# Volume 4 research log — 2 August 2026

Checked current primary documentation for Agent Runtime overview and naming,
deployment routes, custom runtime contract, PSC interface/DNS peering, monitoring,
tracing, logging, revisions/traffic maturity, Cloud Run container/concurrency,
GKE Workload Identity Federation, event services, build provenance, and runtime
locations. Key corrections: Agent Runtime API resources retain the
`ReasoningEngine` name; revisions/traffic are Preview and `v1beta1`; direct
revision calls bypass root traffic rules; custom containers bind to
`0.0.0.0:8080`; and private VPC access is not implicit without PSC configuration.

Implementation decision: ship a deterministic placement/capacity validator and
customer qualification contract, not universal Terraform that would invent the
customer organization, network, identity, region, or state topology.
